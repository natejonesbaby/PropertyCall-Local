{
  "name": "property-call",
  "version": "1.0.0",
  "description": "Automated lead qualification system for real estate investors",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "form-data": "^4.0.5",
    "playwright": "^1.58.0",
    "supertest": "^7.2.2",
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "node-fetch": "^3.3.2",
    "sqlite3": "^5.1.7",
    "ws": "^8.19.0"
  }
}
