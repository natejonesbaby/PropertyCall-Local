# =============================================================================
# Property Call - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000
NODE_ENV=development

# JWT Secret for authentication tokens (use a strong random string in production)
JWT_SECRET=your-jwt-secret-change-this-in-production

# Encryption key for storing API keys securely in database (32+ characters recommended)
ENCRYPTION_KEY=your-encryption-key-change-this-in-production

# -----------------------------------------------------------------------------
# External API Endpoints
# -----------------------------------------------------------------------------
# By default, the app uses production API endpoints. You don't need to set these
# unless you want to override the defaults or use mock servers for testing.
#
# Production Defaults (used when not set):
#   TELNYX_API_BASE       = https://api.telnyx.com
#   DEEPGRAM_API_BASE     = https://api.deepgram.com
#   DEEPGRAM_AGENT_WS_URL = wss://agent.deepgram.com/agent
#   FUB_API_BASE          = https://api.followupboss.com

# -----------------------------------------------------------------------------
# Mock Server Configuration (for local development/testing)
# -----------------------------------------------------------------------------
# Uncomment these lines to use local mock servers instead of real APIs.
# This allows testing without real API keys or making real API calls.
#
# Start mock servers first:
#   node backend/src/mock/telnyx-mock-server.js     # Port 12111
#   node backend/src/mock/deepgram-mock-server.js   # Port 12112
#   node backend/src/mock/fub-mock-server.js        # Port 12113
#
# TELNYX_API_BASE=http://127.0.0.1:12111
# DEEPGRAM_API_BASE=http://127.0.0.1:12112
# DEEPGRAM_AGENT_WS_URL=ws://127.0.0.1:12112/agent
# FUB_API_BASE=http://127.0.0.1:12113

# -----------------------------------------------------------------------------
# Telnyx Configuration (Telephony Provider - Option 1)
# -----------------------------------------------------------------------------
# Get your API key from: https://portal.telnyx.com/#/app/api-keys
# Get phone numbers from: https://portal.telnyx.com/#/app/numbers/my-numbers
#
TELNYX_API_KEY=your_telnyx_api_key
TELNYX_PHONE_NUMBER=+1234567890
TELNYX_WEBHOOK_SECRET=your_webhook_secret

# -----------------------------------------------------------------------------
# SignalWire Configuration (Telephony Provider - Option 2)
# -----------------------------------------------------------------------------
# Alternative to Telnyx. Get credentials from: https://signalwire.com/
# Note: SignalWire credentials are stored in the database via the UI,
# not environment variables. Configure via Settings > API Keys in the app.

# -----------------------------------------------------------------------------
# Deepgram Configuration (Speech-to-Text & Voice AI)
# -----------------------------------------------------------------------------
# Get your API key from: https://console.deepgram.com/
# Used for: Real-time speech recognition, Voice Agent conversations
#
DEEPGRAM_API_KEY=your_deepgram_api_key

# -----------------------------------------------------------------------------
# OpenAI Configuration (LLM for Voice Agent)
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# Used by: Deepgram Voice Agent for conversation intelligence
#
OPENAI_API_KEY=your_openai_api_key

# -----------------------------------------------------------------------------
# Follow-up Boss Configuration (CRM Integration)
# -----------------------------------------------------------------------------
# Get your API key from: https://app.followupboss.com/2/settings/api
# Used for: Lead management, contact syncing, notes
#
FUB_API_KEY=your_followup_boss_api_key
FUB_BASE_URL=https://api.followupboss.com/v1

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# SQLite database file path (relative to backend directory)
DATABASE_PATH=./database.sqlite

# -----------------------------------------------------------------------------
# Email Configuration (Optional - for notifications)
# -----------------------------------------------------------------------------
# Configure SMTP settings for email notifications
#
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_email_password
NOTIFICATION_EMAIL=alerts@example.com

# =============================================================================
# Quick Start Guide
# =============================================================================
#
# FOR PRODUCTION USE:
# 1. Copy this file to .env
# 2. Fill in your real API keys (Telnyx, Deepgram, OpenAI, Follow-up Boss)
# 3. Set strong JWT_SECRET and ENCRYPTION_KEY values
# 4. Start the server: npm run dev
#
# FOR LOCAL TESTING (without real API keys):
# 1. Copy this file to .env
# 2. Uncomment the mock server URLs (TELNYX_API_BASE, DEEPGRAM_*, FUB_API_BASE)
# 3. Start mock servers in separate terminals:
#      node backend/src/mock/telnyx-mock-server.js
#      node backend/src/mock/deepgram-mock-server.js
#      node backend/src/mock/fub-mock-server.js
# 4. Start the server: npm run dev
# 5. Mock servers accept any token starting with "KEY", "MOCK_", or 20+ chars
#
# =============================================================================
